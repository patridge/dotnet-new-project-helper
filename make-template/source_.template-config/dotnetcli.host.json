{
    "$schema": "http://json.schemastore.org/dotnetcli.host",
    "symbolInfo": {
        // This configuration file allows you to manipulate the symbols in the template.json file.
        //"someIdentity": {
        //    "shortName": "id", // Allows setting the `someIdentity` parameter with `-id \"something\"` (note the single hyphen).
        //    "longName": "identity", // Allows setting the `someIdentity` parameter with `--identity \"something\"` (note the double hyphen).
        //},
    },
    "usageExamples": [
        //"", // Usage example shows with no parameters.
        //"--identity \"something cool\"", // Usage example shown with a parameter and value
    ]
}